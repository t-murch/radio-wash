# Test Environment Variables for Integration Tests
# Copy this file to .env.test and fill in your test values
# These values should be for a TEST Supabase instance, not production!

# Test Supabase Configuration
TEST_SUPABASE_URL=http://localhost:54321
TEST_SUPABASE_PUBLIC_URL=http://localhost:54321
TEST_SUPABASE_SERVICE_ROLE_KEY=your-test-service-role-key-here
TEST_SUPABASE_ANON_KEY=your-test-anon-key-here
TEST_SUPABASE_JWT_SECRET=your-test-jwt-secret-at-least-32-chars
TEST_SUPABASE_JWT_ISSUER=supabase-demo

# Note: For local development, you can copy values from your 
# local Supabase setup (appsettings.Development.json)
# For CI, use a dedicated test Supabase project

# Usage:
# Local (host-based): source .env.test && dotnet test --filter Integration
# CI (containerized): docker compose -f docker-compose.test.yml up --build --abort-on-container-exit